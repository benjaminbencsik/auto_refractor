
function cleanFunction() {
    // A proper logger should be used instead of console.log
    // logger.info('This line is now shorter and more readable.');
    const x = 1;
    const y = 2;
    const z = x + y;
    // logger.info(z);
    return z;
}

cleanFunction();
